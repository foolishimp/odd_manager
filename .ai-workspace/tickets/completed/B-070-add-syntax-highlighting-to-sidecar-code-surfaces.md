---
id: B-070
title: Add syntax highlighting to Sidecar code surfaces
type: feature
ticket_category: ordinary
status: completed
goal: realize-ai-workspace-topology-and-agent-interoperability
change_intent: Render markdown code fences and direct source-code files with theme-aware syntax highlighting.
change_class: realization_refactor
re_entry_point: realization
affected_boundary: build_tenants/react_vite/src/components/MarkdownDocument.tsx, build_tenants/react_vite/src/components, build_tenants/react_vite/src/features/sidecar/SidecarPanel.tsx, build_tenants/react_vite/src/app/styles.css, build_tenants/react_vite/tests/e2e/odd-manager-smoke.spec.ts
priority: high
triaged_at: 2026-04-29
created_at: 2026-04-29
updated_at: 2026-05-01T00:55:00+10:00
build_tenant: react_vite
sprint: SPRINT-2026-04-30-sidecar-document-viewer
review_status: accepted_by_code_review
dependencies:
  - B-066 completed
governance_scope: STDO-UX Method
governance_scope_expansion:
  - S: SPEC_METHOD.md
  - T: TICKET_METHOD.md
  - U: UX_METHOD.md
ux_surface_scope: Sidecar markdown and source-code document surfaces
library_usage: evaluate_and_consume
governing_library_candidate: shiki
library_rationale: Candidate library uses TextMate grammar/theme assets compatible with the VS Code mental model and supports explicit light/dark theme selection.
intake_source: Operator request for syntax highlighting for at least Python, TypeScript/JavaScript, JSON, YAML, Java, Scala, and Rust.
target_truth: Code fences and source files render with readable, theme-aware syntax highlighting for the required languages, with plain-text fallback for unknown languages.
superseded_truth: Non-Mermaid code fences render as plain `<pre><code>` blocks, and direct source files are treated like markdown text.
closure_law: This ticket closes only when syntax highlighting is library-backed, theme-aware, tested for required languages, and implemented through the shared document viewer rather than Sidecar-specific string manipulation.
evaluation_criteria:
  - required languages are mapped: python, typescript, javascript, json, yaml, java, scala, rust
  - unknown languages fall back to plain text without throwing
  - markdown code fences highlight through the same code-rendering adapter used for source files
  - direct `.py`, `.ts`, `.tsx`, `.js`, `.json`, `.yaml`, `.yml`, `.java`, `.scala`, and `.rs` files render as code documents
  - highlighting follows current light, dark-blue, and dark-grey theme selection
  - bundle strategy avoids importing every language/theme eagerly when not needed
  - rendered highlighted HTML is sanitized or generated by the approved library path only
  - Playwright proof opens at least one markdown fixture and one direct source fixture
  - `npm run build` passes
  - `npm run test:sidecar-wave` passes
  - `npm run test:e2e` passes
proof_surface:
  - build_tenants/react_vite/src/components/MarkdownDocument.tsx
  - build_tenants/react_vite/src/components
  - build_tenants/react_vite/src/features/sidecar/SidecarPanel.tsx
  - build_tenants/react_vite/src/app/styles.css
  - build_tenants/react_vite/tests/e2e/odd-manager-smoke.spec.ts
non_closure_conditions:
  - syntax highlighting is implemented with local regex tokenizers
  - language detection depends on ad hoc path substring checks where extension parsing is available
  - dark-grey theme produces bright/clashing code blocks
  - code files still render as markdown paragraphs
---

## SPEC_METHOD Triage

This is a realization refactor after B-066. The desired capability is stable:
source and documentation surfaces should be inspectable. The work replaces
plain code blocks with a library-backed code viewer adapter.

Lawful re-entry point: Realization.

## Sprint Boundary

This ticket is admitted into
`.ai-workspace/sprints/SPRINT-2026-04-30-sidecar-document-viewer.md` after the
B-066 design gate. It may use UX sprint compliance escrow for screenshots,
walkthrough notes, accessibility review, and Msg-replay updates, but language
support, theme bundles, and highlighted HTML safety must follow the shared
`DocumentViewer` carrier.

## Backlog Review 2026-05-01

No `shiki` backed shared code adapter is implemented yet. Code fences and
source files remain plain text, so this ticket stays open in the
document-viewer sprint.

## Priority Reprice 2026-05-01

This ticket is in the active focus set with B-067 and B-068.

## Closure Review 2026-05-01

Accepted. Markdown code fences and direct source files now use the shared
`DocumentViewer` code adapter backed by Shiki. The implementation maps the
required language family plus TSX/JSX/Markdown, falls back to plain code blocks
for unknown languages, and imports only the focused language/theme bundles
rather than the whole Shiki registry.
